[package]
name = "propchain-bridge"
version = "1.0.0"
authors = ["PropChain Team <dev@propchain.io>"]
edition = "2021"
description = "Cross-chain bridge contract for controlled property-token transfers"
license = "MIT"
homepage = "https://propchain.io"
repository = "https://github.com/MettaChain/PropChain-contract"
publish = false

[dependencies]
soroban-sdk = "20.0.0"
# Shared two-step, time-locked admin transfer — one implementation across every
# Soroban contract in the repository rather than a copy per crate.
stellar-insured-lib = { path = "../lib" }

[lib]
name = "propchain_bridge"
path = "src/lib.rs"
crate-type = ["cdylib"]

[features]
default = ["std"]
std = ["soroban-sdk/testutils"]
